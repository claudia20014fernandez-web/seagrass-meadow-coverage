# Data folder

Place your input files here (they are not versioned in Git):

- `my_video.mp4`  — your underwater video (the original from the camera, for telemetry)
- `my_track.gpx`  — your GPX file, only if you use `gps.source: gpx`

Adjust the paths in `configs/config.yaml`.
